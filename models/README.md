# Modelos e pesos

Pesos de modelos nao devem ser versionados neste repositorio. Documente origem, versao, licenca,
integridade e procedimento de obtencao em `docs/entrega/recursos-externos.md` e, quando aplicavel,
automatize a obtencao em `scripts/`.
