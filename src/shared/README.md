# Shared utilities

Use this directory only for small utilities genuinely shared by multiple modules. Contracts live
in `src/contracts/`; business logic belongs to its owning module. Do not turn this directory into
a dependency dumping ground.
